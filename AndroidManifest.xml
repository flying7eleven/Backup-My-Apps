<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.halcyonwaves.apps.backupmyapps" android:versionCode="60" android:versionName="0.6">
    
    <!-- define for which versions the application is -->
    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="15" android:maxSdkVersion="15" />
    
    <!-- define the content of the application package -->
    <application android:icon="@drawable/ic_launcher" android:label="@string/app_name">
		<activity android:name=".StartupActivity" android:label="@string/app_name">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    
</manifest>